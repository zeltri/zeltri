---
import Layout from "../../layouts/Layout.astro";
---

<Layout title="Inicio Rápido - Bunny-JS">
  <style>
    .page {
      max-width: 800px;
      margin: 0 auto;
      padding: 3rem 2rem;
      background: #0f0f0f;
      min-height: 100vh;
    }

    h1 {
      font-size: 2rem;
      color: #fafafa;
      margin-bottom: 1rem;
      border-bottom: 1px solid #262626;
      padding-bottom: 1rem;
    }

    h2 {
      font-size: 1.3rem;
      color: #f59e0b;
      margin-top: 2rem;
      margin-bottom: 1rem;
    }

    p,
    li {
      color: #a3a3a3;
      line-height: 1.7;
      margin-bottom: 1rem;
    }

    ol {
      margin-left: 1.5rem;
    }

    li {
      margin-bottom: 1rem;
    }

    code {
      background: #1a1a1a;
      color: #fbbf24;
      padding: 0.2rem 0.4rem;
      border-radius: 0.2rem;
      font-family: "Monaco", monospace;
    }

    pre {
      background: #1a1a1a;
      border: 1px solid #262626;
      padding: 1rem;
      border-radius: 0.5rem;
      overflow-x: auto;
      margin: 1rem 0;
    }

    pre code {
      background: none;
      color: #fbbf24;
      padding: 0;
    }

    .step {
      background: #1a1a1a;
      border-left: 3px solid #f59e0b;
      padding: 1.5rem;
      margin: 1.5rem 0;
      border-radius: 0.25rem;
    }

    .step h3 {
      color: #f59e0b;
      margin: 0 0 0.75rem 0;
    }

    .nav {
      display: flex;
      justify-content: space-between;
      margin-top: 3rem;
      padding-top: 2rem;
      border-top: 1px solid #262626;
    }

    a {
      color: #f59e0b;
      text-decoration: none;
    }

    a:hover {
      text-decoration: underline;
    }
  </style>

  <div class="page">
    <h1>🚀 Inicio Rápido (5 minutos)</h1>

    <h2>Instalación</h2>
    <pre><code>npm install bunny-js
# o con pnpm
pnpm add bunny-js
# o con yarn
yarn add bunny-js</code></pre>

    <div class="step">
      <h3>Paso 1: Importar</h3>
      <pre><code>import BunnyJS from 'bunny-js';</code></pre>
    </div>

    <div class="step">
      <h3>Paso 2: Crear un elemento</h3>
      <pre><code>const btn = BunnyJS.button();
btn.text("Haz clic");
btn.insertIn(document.body);</code></pre>
    </div>

    <div class="step">
      <h3>Paso 3: Agregar estilos</h3>
      <pre><code>btn.bg("#f59e0b").color("black").font("bold");</code></pre>
    </div>

    <div class="step">
      <h3>Paso 4: Agregar eventos</h3>
      <pre><code is:raw>btn.on("click", () => {
  console.log("¡Clickeado!");
});</code></pre>
    </div>

    <div class="step">
      <h3>Paso 5: Todo junto (encadenado)</h3>
      <pre><code>BunnyJS.button()
  .text("Haz clic")
  .bg("#f59e0b")
  .color("black")
  .font("bold")
  .on("click", () => console.log("¡Hola!"))
  .insertIn(document.body);</code></pre>
    </div>

    <h2>Conceptos clave</h2>
    <ul>
      <li>
        <strong>Todo es un método:</strong> Cada operación retorna el elemento
      </li>
      <li><strong>Encadenamiento:</strong> Encadena métodos sin límite</li>
      <li>
        <strong>Selección:</strong>
        <code>.select(".class")</code> o <code>.selectAll("p")</code>
      </li>
      <li>
        <strong>Contenido:</strong>
        <code>.text("hola")</code> o <code>.link("text", "url")</code>
      </li>
      <li><strong>Eventos:</strong> <code>.on("click", handler)</code></li>
    </ul>

    <h2>Ejemplos prácticos</h2>

    <p><strong>Crear un formulario:</strong></p>
    <pre><code is:raw>const form = BunnyJS.form();
BunnyJS.input({ type: "text", placeholder: "Nombre" }).insertIn(form);
BunnyJS.button().text("Enviar").insertIn(form);
form.insertIn(document.body);</code></pre>

    <p><strong>Manipular elementos existentes:</strong></p>
    <pre><code>const heading = BunnyJS.select("h1");
heading.text("Nuevo título").color("#f59e0b");</code></pre>

    <p><strong>Lista dinámica:</strong></p>
    <pre><code is:raw>const list = BunnyJS.ul();
["Elemento 1", "Elemento 2", "Elemento 3"].forEach(text => {
  BunnyJS.li().text(text).insertIn(list);
});
list.insertIn(document.body);</code></pre>

    <div class="nav">
      <a href="/bunny-js">← Volver al inicio</a>
      <a href="/bunny-js/api">API Referencia →</a>
    </div>
  </div>
</Layout>
